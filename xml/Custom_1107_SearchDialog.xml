<?xml version="1.0" encoding="UTF-8"?>
<window type="dialog" id="1107">
	<defaultcontrol always="true">9000</defaultcontrol>
	<include>Animation_DialogPopupOpenClose</include>
	<controls>
		<control type="group">
			<centerleft>50%</centerleft>
			<width>600</width>
			<centertop>60%</centertop>
			<height>700</height>
			<include content="DialogBackgroundCommons">
				<param name="width" value="600" />
				<param name="height" value="80" />
				<param name="header_label" value="$LOCALIZE[137]" />
				<param name="header_id" value="" />
			</include>
			<control type="panel" id="9000">
				<include>ButtonMenuList</include>
				<content>
					<item>
						<label>$LOCALIZE[31113]</label>
						<onclick>Dialog.Close(all)</onclick>
						<onclick condition="System.AddonIsEnabled(script.globalsearch)">RunScript(script.globalsearch)</onclick>
						<onclick condition="System.HasAddon(script.globalsearch) + !System.AddonIsEnabled(script.globalsearch)">EnableAddon(script.globalsearch)</onclick>
						<onclick condition="!System.HasAddon(script.globalsearch)">InstallAddon(script.globalsearch)</onclick>
					</item>
					<item>
						<label>$LOCALIZE[31145]</label>
						<onclick>Dialog.Close(all)</onclick>
						<onclick>ActivateWindow(addonbrowser,addons://search/,return)</onclick>
					</item>
					<!-- Kofin's own search, over the Jellyfin server. With the
					     other add-on search below it, after the two Kodi-native
					     ones. It covers what script.globalsearch cannot: a
					     Jellyfin library that is browsed rather than synced has
					     no Kodi rows to find.

					     ActivateWindow rather than the RunPlugin the home
					     buttons use — this route answers with a listing, not a
					     dialog. No InstallAddon arm either: Kofin is not in the
					     official repository, so the entry hides instead of
					     offering an install that cannot succeed. -->
					<item>
						<label>$LOCALIZE[31615]</label>
						<onclick>Dialog.Close(all)</onclick>
						<onclick condition="System.AddonIsEnabled(plugin.video.kofin)">ActivateWindow(videos,"plugin://plugin.video.kofin/?mode=search",return)</onclick>
						<onclick condition="System.HasAddon(plugin.video.kofin) + !System.AddonIsEnabled(plugin.video.kofin)">EnableAddon(plugin.video.kofin)</onclick>
						<visible>System.HasAddon(plugin.video.kofin)</visible>
					</item>
					<item>
						<label>$LOCALIZE[31114]</label>
						<onclick>Dialog.Close(all)</onclick>
						<onclick condition="System.AddonIsEnabled(plugin.video.youtube)">ActivateWindow(videos,"plugin://plugin.video.youtube/kodion/search/list/",return)</onclick>
						<onclick condition="System.HasAddon(plugin.video.youtube) + !System.AddonIsEnabled(plugin.video.youtube)">EnableAddon(plugin.video.youtube)</onclick>
						<onclick condition="!System.HasAddon(plugin.video.youtube)">InstallAddon(plugin.video.youtube)</onclick>
					</item>
				</content>
			</control>
		</control>
	</controls>
</window>
